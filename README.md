# Frontend for a distributed Mandelbrot renderer

## Goal of the project
Get more familiar with `React` and `WebSockets`

## Usage

### Start the React app
`npm run start`


### Start the Websocket (Mock) backend
`cd websocket`
`npm run start`
